if [ $LNX ]; then
  if [ -f /etc/bash_completion ]; then
    source /etc/bash_completion
  fi
fi

if [ $OSX ]; then
  if [ -f `brew --prefix`/etc/bash_completion ]; then
    source `brew --prefix`/etc/bash_completion
  fi
fi

for i in $HOME/.bash/completion.d/*; do
  source $i
done
